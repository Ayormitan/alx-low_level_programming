#include <stdio.h>
#include "main.h"
/**
 * main - code test
 *
 * Return: Always success
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
